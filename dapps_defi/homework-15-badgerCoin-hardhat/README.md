# Deploy BadgerCoin using Hardhat

1. Run tests with `npx hardhat test`
2. Set privateKey in `secrets.json`
3. Configure networks in `hardhat.config.js`
4. Deploy using deploy script `npx hardhat run scripts/deploy.js`